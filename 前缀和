//思想:快速算出某一个区间的所有数之和 （静态区间）
使用一个数组s[N]来表示原数组每一个区间[l,r]从l位置开始的和,a[N]表示原数组
s[N] = s[N - 1] + a[N];
因此，要求s[N]，先求s[l]的值
for(int i = l - 1;i <= r;i ++ )
{
     s[i]  = s[i - 1] + a[i];
}
最后输出是s[r]  - s[l - 1] （指 l 到 r 的值和）的值
for(int i = 0;i <= N;i ++)
{
     printf(
}

